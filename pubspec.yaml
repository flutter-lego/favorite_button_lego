name: favorite_button_lego
description: "favorite button lego under simgple architecture framework."
#publish_to: 'none'
repository: https://github.com/flutter-lego/favorite_button_lego

version: 1.0.5

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: #@add
  gap: #@add
  june_flow_util:
  styled_widget: #@add
  modal_bottom_sheet: #@add
  keyboard_attachable: #@add
  flutter_zoom_drawer: #@add
  flutter_screenutil:
  bot_toast:
  june:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  screenutil_lego: #@add
  bot_toast_lego: #@add
  widget_binding_lego: #@add
  usage_note_lego:
  june_lego:

flutter:
  assets:
    - assets/lego/favorite_button_lego/
  uses-material-design: true

topics:
  - lego